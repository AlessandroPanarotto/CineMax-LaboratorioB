/**
 * Package radice dell'applicazione CineMax (Laboratorio Interdisciplinare B).
 *
 * <p>Conterra' le classi di modello, i servizi di back-end ({@code serverCM})
 * e il client ({@code clientCM}) per la gestione di palinsesto e prenotazioni
 * del cinema CineMax, con persistenza su PostgreSQL via JDBC.</p>
 */
package cinemax;
